# Update Environment Configuration: Update .env file to ensure that the frontend API calls hit the appropriate backend URL.
# You can put the backend server URL in .env file:
REACT_APP_BACKEND_URL=http://localhost:3001
